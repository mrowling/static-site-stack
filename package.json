{
    "name": "static-site-stack",
    "private": true,
    "packageManager": "pnpm@10.23.0",
    "scripts": {
        "build": "pnpm -r build",
        "build:web": "pnpm --filter static-site-stack-web build",
        "build:infra": "pnpm --filter static-site-stack-infra build",
        "test": "pnpm -r test",
        "deploy": "pnpm --filter static-site-stack-infra cdk deploy"
    }
}